package org.newdeal.core.bean;

/**
 * -- UNSTABLE --
 *
 * Type of entities basically managed by the system.
 *
 * @author Frederick Vanderbeke
 * @since 22/06/2015.
 * @version 0.0.1
 */
public enum BeanType {
    PROXY_ENGINE,
    ROUTING_ENGINE,
    RESOURCE_ENGINE,
    DISPATCH_ENGINE,
    BRIDGE_ENGINE,
    CENTER,
    ENVIRONMENT,
    PROXY,
    BRIDGE,
    RESOURCE_PROVIDER,
    INTERCEPTOR,
    SERVICE,
    STAGE,
    NETWORK,
    SERVER
}
